server:
  port: 9001
spring:
  application:
     name: consumer-service
  datasource:
    url: jdbc:mysql://localhost:3306/consumer-data
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    properties:
         hibernate:
            dialect: org.hibernate.dialect.MySQL8Dialect
        
  